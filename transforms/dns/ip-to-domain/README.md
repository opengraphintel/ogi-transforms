# IP to Domain (Reverse DNS)

Performs a reverse DNS (PTR) lookup on an IP address to discover associated domain names.

## Input / Output

- **Input**: `IPAddress`
- **Output**: `Domain` entities

## API Keys

None required.
