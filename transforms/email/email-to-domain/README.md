# Email to Domain

Extracts the domain portion from an email address. A simple but useful transform for pivoting from email addresses to domain investigation.

## Input / Output

- **Input**: `EmailAddress` (e.g., `admin@example.com`)
- **Output**: `Domain` (e.g., `example.com`)

## API Keys

None required. No network access needed.
