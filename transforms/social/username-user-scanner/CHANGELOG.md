# Changelog

## 1.0.0
- Initial release.
- Username presence scan using `user-scanner` library mode (`check_all` / `check_category`).
- Maps found profiles to `SocialMedia` and `URL` entities with relationship edges.
- Supports scan scope, only-found filtering, and max-results limiting.
- Includes unit tests for found mapping, result limits, and failure handling.
